# Production Environment Variables for Backend

# MongoDB Atlas (Already configured - keep as is)
MONGO_URI=mongodb+srv://admin:1234@ise.qxi98tc.mongodb.net/?retryWrites=true&w=majority

# JWT Secret (Generate new one for production!)
JWT_SECRET=your_super_secure_production_jwt_secret_key_here_make_it_very_long

# Email Configuration (Brevo SMTP)
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USER=972cc2001@smtp-brevo.com
EMAIL_PASS=your_brevo_api_key_here
EMAIL_FROM=noreply@scipark.com

# Server Configuration
PORT=3000
NODE_ENV=production

# Frontend URL (Update this after deploying frontend)
CLIENT_URL=https://your-frontend-name.netlify.app

# Or if using Vercel
# CLIENT_URL=https://your-frontend-name.vercel.app
