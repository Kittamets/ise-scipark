# 🔄 SciPark Process Flow Documentation

## 📊 As-Is vs To-Be Comparison

### ❌ As-Is Process Flow (ปัจจุบัน - มีปัญหา)

```mermaid
graph LR
    Start([ผู้ใช้มาถึงคณะ]) --> SelectZone[เลือกโซนจอดรถ]
    SelectZone --> CheckAvailable{มีที่จอดในโซน<br/>นี้ไหม?}
    CheckAvailable -->|ไม่มี| SelectZone
    CheckAvailable -->|มี| SelectSpot[เลือกที่จอด]
    SelectSpot --> Enter[เข้าจอด]
    Enter --> End([ผู้ใช้จอดรถ])
    
    style Start fill:#e3f2fd
    style End fill:#c8e6c9
    style CheckAvailable fill:#ffccbc
    style SelectZone fill:#fff9c4
    style SelectSpot fill:#fff9c4
```

#### 🔴 Pain Points ของ As-Is:

**1. ผู้ใช้เลือกโซนที่จอด**
- ❌ ต้องเดินทางมาที่คณะก่อน
- ❌ ไม่รู้ว่ามีที่จอดหรือไม่
- ❌ หากไม่มีที่ว่าง ต้องวนหาโซนใหม่
- ⏰ เสียเวลาเพิ่ม 15-30 นาที

**2. ผู้ใช้เลือกที่จอด**
- ❌ ต้องหาที่ว่างเอง
- ❌ ไม่แน่ใจว่าที่จอดนั้นดีหรือไม่

---

### ✅ To-Be Process Flow (หลังใช้ SciPark - แก้ปัญหา)

```mermaid
graph LR
    Start([ผู้ใช้เตรียมมาคณะ]) --> Find[หาที่จอด<br/>อัตโนมัติ]
    Find --> Book[จองที่จอด]
    Book --> Pay[ชำระเงิน]
    Pay --> Enter[เข้าจอด]
    Enter --> End([ผู้ใช้จอดรถ])
    
    style Start fill:#e3f2fd
    style End fill:#c8e6c9
    style Find fill:#a5d6a7
    style Book fill:#a5d6a7
    style Pay fill:#90caf9
    style Enter fill:#a5d6a7
```

#### 🟢 Solutions ของ To-Be:

**Process 1: หาที่จอด** 🔍
- ✅ หาที่ว่างโดยอัตโนมัติ
- ✅ แสดงจำนวนที่ว่างแบบ Real-time
- ✅ ระบบเลือกที่จอดให้เอง
- ✅ มั่นใจว่าที่จอดมีคุณภาพ

**Process 2: จองที่จอด** 📱
- ✅ จองล่วงหน้าผ่านแอป
- ✅ มั่นใจว่าไปถึงมีที่จอดแน่นอน
- ✅ วางแผนการเดินทางได้

**Process 3: เข้าจอด** 🚗
- ✅ มั่นใจว่าที่จอดพร้อมใช้งาน
- ✅ มี Alert เมื่อเกิดปัญหา
- ✅ QR Code ยืนยันการเข้าจอด

---

## 🔄 Detailed Process Flows

### 1. หาที่จอด (Find Parking Spot)

```mermaid
flowchart TD
    Start([เริ่มต้น]) --> Open[เปิดแอป SciPark]
    Open --> SelectZone[เลือกโซนที่ต้องการ<br/>หรือปล่อยให้ระบบเลือก]
    SelectZone --> CheckReal{ตรวจสอบที่ว่าง<br/>Real-time}
    CheckReal -->|มีที่ว่าง| ShowAvailable[แสดงจำนวนที่ว่าง<br/>และรายละเอียดโซน]
    CheckReal -->|ไม่มีที่ว่าง| Suggest[แนะนำโซนอื่น<br/>ที่มีที่ว่าง]
    Suggest --> ShowAvailable
    ShowAvailable --> AutoSelect[ระบบเลือกที่จอด<br/>ที่ดีที่สุดให้อัตโนมัติ]
    AutoSelect --> ShowDetails[แสดงรายละเอียด:<br/>- ชื่อที่จอด<br/>- ตำแหน่ง<br/>- ราคา<br/>- สิ่งอำนวยความสะดวก]
    ShowDetails --> Confirm{ผู้ใช้ยืนยัน?}
    Confirm -->|ใช่| NextBook[ไปขั้นตอนจอง]
    Confirm -->|ไม่| SelectZone
    
    style Start fill:#e3f2fd
    style CheckReal fill:#fff9c4
    style AutoSelect fill:#a5d6a7
    style ShowDetails fill:#90caf9
    style Confirm fill:#ffccbc
    style NextBook fill:#c8e6c9
```

**Key Features:**
- 📊 Real-time availability checking
- 🤖 Auto-selection algorithm
- 🎯 Best spot recommendation
- 📍 Location details
- 💰 Price transparency

---

### 2. จองที่จอด (Book Parking Spot)

```mermaid
flowchart TD
    Start([เริ่มจอง]) --> CheckAuth{ล็อกอินแล้ว?}
    CheckAuth -->|ไม่| Login[Login/Register]
    CheckAuth -->|แล้ว| SelectTime[เลือกเวลาที่จะมา]
    Login --> SelectTime
    SelectTime --> SelectVehicle[เลือกยานพาหนะ]
    SelectVehicle --> ConfirmBooking[ยืนยันการจอง]
    ConfirmBooking --> CreateBooking[สร้าง Booking<br/>ในระบบ]
    CreateBooking --> LockSpot[ล็อคที่จอด<br/>ไว้ให้ผู้ใช้]
    LockSpot --> ShowBookingID[แสดง Booking ID<br/>และ QR Code]
    ShowBookingID --> SetTimer[เริ่มนับเวลา<br/>30 นาที]
    SetTimer --> Notify[ส่ง Notification<br/>ยืนยันการจอง]
    Notify --> End([รอเข้าจอด])
    
    style Start fill:#e3f2fd
    style CheckAuth fill:#fff9c4
    style CreateBooking fill:#a5d6a7
    style LockSpot fill:#a5d6a7
    style ShowBookingID fill:#90caf9
    style Notify fill:#c8e6c9
    style End fill:#c8e6c9
```

**Business Rules:**
- ⏱️ **30 นาที** ต้องเข้าจอดหลังจอง (ไม่งั้นยกเลิกอัตโนมัติ)
- 🎯 **1 Booking** ต่อ 1 User ในเวลาเดียวกัน
- 🔒 ที่จอดจะ**ถูกล็อค**ไว้ให้ผู้จอง
- 📱 รับ **Notification** ยืนยันทันที

---

### 3. ชำระเงิน (Payment Process)

```mermaid
flowchart TD
    Start([เข้าสู่หน้าชำระเงิน]) --> CheckTime[ตรวจสอบเวลา<br/>ที่จอด]
    CheckTime --> Calculate[คำนวณค่าบริการ<br/>- ชั่วโมงแรกฟรี<br/>- ชั่วโมงถัดไป 20฿/ชม.]
    Calculate --> CheckMember{มี Membership?}
    CheckMember -->|Diamond| Discount10[ส่วนลด 10%]
    CheckMember -->|Predator| Discount15[ส่วนลด 15%]
    CheckMember -->|Iron| NoDiscount[ไม่มีส่วนลด]
    Discount10 --> ShowTotal
    Discount15 --> ShowTotal
    NoDiscount --> ShowTotal[แสดงยอดรวม]
    ShowTotal --> SelectMethod[เลือกวิธีชำระเงิน:<br/>- บัตรเครดิต/เดบิต<br/>- Mobile Banking<br/>- QR Payment]
    SelectMethod --> ProcessPayment[ดำเนินการชำระเงิน]
    ProcessPayment --> Verify{ชำระสำเร็จ?}
    Verify -->|สำเร็จ| Receipt[สร้างใบเสร็จ]
    Verify -->|ไม่สำเร็จ| Error[แสดง Error<br/>ลองใหม่อีกครั้ง]
    Error --> SelectMethod
    Receipt --> UpdateBooking[อัปเดตสถานะ<br/>การจองเป็น Completed]
    UpdateBooking --> SendReceipt[ส่งใบเสร็จ<br/>ทาง Email/App]
    SendReceipt --> End([เสร็จสิ้น])
    
    style Start fill:#e3f2fd
    style Calculate fill:#fff9c4
    style CheckMember fill:#ffccbc
    style ShowTotal fill:#90caf9
    style ProcessPayment fill:#fff9c4
    style Verify fill:#ffccbc
    style Receipt fill:#a5d6a7
    style End fill:#c8e6c9
```

**Pricing Rules:**
- 💳 **ค่าธรรมเนียมการจอง: 20 ฿/ครั้ง** (หักทันทีเมื่อจอง)
- 🆓 **3 ชั่วโมงแรกฟรี** สำหรับทุกคน
- 💰 **10 ฿/ชม.** สำหรับชั่วโมงที่เกิน (ชั่วโมงที่ 4, 5, 6...)
- ⏱️ **ต้องเข้าจอดภายใน 30 นาที** หลังจอง (ไม่งั้นยกเลิกอัตโนมัติ)
- 👑 **ส่วนลดตาม Tier (Monthly Membership - Phase 3):**
  - Iron: 0% (ฟรี)
  - Diamond: 10% (299 ฿/เดือน)
  - Predator: 15% (599 ฿/เดือน)

---

### 4. เข้าจอด (Enter Parking)

```mermaid
flowchart TD
    Start([มาถึงคณะ]) --> CheckBooking{มี Booking?}
    CheckBooking -->|ไม่มี| NoBooking[ไม่สามารถเข้าได้]
    CheckBooking -->|มี| ShowQR[แสดง QR Code<br/>บนแอป]
    ShowQR --> ScanQR[Scan QR Code<br/>ที่จุดเข้าจอด]
    ScanQR --> Verify{ยืนยันการจอง}
    Verify -->|ไม่ถูกต้อง| Error[แสดง Error]
    Verify -->|ถูกต้อง| StartTimer[เริ่มนับเวลาจอด]
    StartTimer --> ShowLocation[แสดงตำแหน่ง<br/>ที่จอดบนแอป]
    ShowLocation --> OpenGate[เปิดไม้กั้น]
    OpenGate --> Navigate[นำทางไปที่จอด<br/>ด้วยแผนที่]
    Navigate --> Park[จอดรถ]
    Park --> SendAlert[ส่ง Alert<br/>เข้าจอดสำเร็จ]
    SendAlert --> Monitor[ระบบติดตามเวลา<br/>และแจ้งเตือน]
    Monitor --> End([กำลังจอดอยู่])
    
    style Start fill:#e3f2fd
    style CheckBooking fill:#ffccbc
    style Verify fill:#ffccbc
    style StartTimer fill:#a5d6a7
    style ShowLocation fill:#90caf9
    style Park fill:#a5d6a7
    style End fill:#c8e6c9
```

**Entry Process:**
- 📲 แสดง **QR Code** จากแอป
- 🔍 Scan ที่**จุดเข้าจอด**
- ✅ **ยืนยันการจอง** อัตโนมัติ
- ⏱️ **เริ่มนับเวลา** ทันที
- 🗺️ **นำทาง** ไปยังที่จอด
- 🔔 **Alert** เมื่อมีปัญหา

---

### 5. ออกจากที่จอด (Exit Parking)

```mermaid
flowchart TD
    Start([ผู้ใช้กดจบการจอด]) --> CalcTime[คำนวณเวลา<br/>ที่จอดทั้งหมด]
    CalcTime --> CheckPayment{ชำระเงินแล้ว?}
    CheckPayment -->|แล้ว| CheckOver{จอดเกินเวลา?}
    CheckPayment -->|ยังไม่| GoPayment[ไปหน้าชำระเงิน]
    GoPayment --> Payment[Process Payment]
    CheckOver -->|ไม่เกิน| Free[ออกได้เลย<br/>ไม่มีค่าใช้จ่ายเพิ่ม]
    CheckOver -->|เกิน| CalcExtra[คำนวณค่าใช้จ่ายเพิ่ม]
    CalcExtra --> PayExtra[ชำระส่วนเกิน]
    PayExtra --> Release
    Free --> Release[ปลดล็อคที่จอด]
    Payment --> Release
    Release --> ShowSummary[แสดงสรุป:<br/>- เวลาที่จอด<br/>- ค่าใช้จ่ายรวม<br/>- แต้มที่ได้รับ]
    ShowSummary --> UpdateStats[อัปเดตสถิติผู้ใช้<br/>และให้แต้ม]
    UpdateStats --> SendReceipt[ส่งใบเสร็จ]
    SendReceipt --> OpenExit[เปิดไม้กั้นทางออก]
    OpenExit --> Thanks[ขอบคุณที่ใช้บริการ]
    Thanks --> End([เสร็จสิ้น])
    
    style Start fill:#e3f2fd
    style CalcTime fill:#fff9c4
    style CheckPayment fill:#ffccbc
    style CheckOver fill:#ffccbc
    style Release fill:#a5d6a7
    style ShowSummary fill:#90caf9
    style End fill:#c8e6c9
```

**Exit Rules:**
- ✅ ต้อง**ชำระเงิน**ก่อนออก
- 💰 คำนวณ**ค่าเกิน**ถ้าจอดนานกว่าที่จอง
- 🎁 ได้รับ**แต้มสะสม**ทุกครั้ง
- 📧 ส่ง**ใบเสร็จ**อัตโนมัติ

---

## 📊 System Integration Flow

```mermaid
flowchart TB
    User[ผู้ใช้งาน] -->|เข้าใช้งาน| WebApp[Web Application<br/>React + Vite]
    WebApp -->|API Calls| Backend[Backend API<br/>Express.js]
    Backend -->|Query/Update| MongoDB[(MongoDB<br/>Database)]
    Backend -->|Send Email| Email[Email Service<br/>Nodemailer + Brevo]
    Backend -->|Authenticate| JWT[JWT Token<br/>HTTP-only Cookies]
    WebApp -->|Real-time| Socket[WebSocket<br/>สำหรับ Real-time Updates]
    
    style User fill:#e3f2fd
    style WebApp fill:#90caf9
    style Backend fill:#a5d6a7
    style MongoDB fill:#fff9c4
    style Email fill:#ffccbc
    style JWT fill:#ce93d8
```

---

## 🎯 Key Improvements (As-Is → To-Be)

| Aspect | ❌ As-Is | ✅ To-Be | 📈 Improvement |
|--------|---------|---------|----------------|
| **หาที่จอด** | ต้องมาดูเอง, ไม่รู้ว่ามีที่ว่างไหม | ดูได้จากแอป, Real-time | ⏰ -70% เวลา |
| **จองที่จอด** | ใครไว ใครได้ | จองล่วงหน้าได้ | ✅ มั่นใจ 100% |
| **เข้าจอด** | ไม่แน่ใจ, ไม่มี Alert | QR Code + Alert อัตโนมัติ | 🔔 Alert Real-time |
| **ชำระเงิน** | ชำระเงินสด | หลายช่องทาง, อัตโนมัติ | 💳 สะดวก, ปลอดภัย |
| **ประสบการณ์** | เครียด, เสียเวลา | สะดวก, มั่นใจ | 😊 พึงพอใจ +80% |

---

## 📝 Summary

SciPark **แก้ปัญหา Pain Points** ทั้งหมดของระบบเดิม:

### Before (As-Is):
- ❌ ไม่รู้ว่ามีที่จอดไหม
- ❌ ต้องวนหาที่จอด
- ❌ เสียเวลา 20-30 นาที
- ❌ ไม่สามารถจองได้

### After (To-Be):
- ✅ รู้ข้อมูลแบบ Real-time
- ✅ ระบบหาที่จอดให้อัตโนมัติ
- ✅ ประหยัดเวลา 70%
- ✅ จองล่วงหน้าได้
- ✅ มั่นใจว่ามีที่จอดรอรับ
- ✅ ชำระเงินสะดวก
- ✅ มี Alert แจ้งเตือน

---

## 💰 Pricing Summary

### Pay-per-Booking Model:
```
ค่าจอง (Booking Fee):        20 ฿/ครั้ง
3 ชั่วโมงแรก:                ฟรี
ชั่วโมงที่เกิน:               10 ฿/ชม.

ตัวอย่าง:
├─ จอด 2 ชม.  → 20 ฿ (ค่าจองอย่างเดียว)
├─ จอด 4 ชม.  → 30 ฿ (20 + 10×1)
└─ จอด 6 ชม.  → 50 ฿ (20 + 10×3)
```

### Monthly Membership (Phase 3):
```
🥉 Iron       → ฟรี       (ไม่มีส่วนลด)
💎 Diamond    → 299 ฿/เดือน (ลด 10%)
👹 Predator   → 599 ฿/เดือน (ลด 15% + Priority)
```

---

## 🎯 Key Decision Points

### 1. Decision: โซนเต็ม → แนะนำโซนอื่น
```
หากโซนที่เลือกไม่มีที่ว่าง:
✓ ระบบแสดงโซนทางเลือก (เรียงตามระยะทาง)
✓ แสดงจำนวนที่ว่างของแต่ละโซน
✓ แสดงเวลาที่คาดว่าจะว่าง (ถ้ามีข้อมูล)
✓ ผู้ใช้เลือกโซนใหม่ หรือรอโซนเดิมว่าง
```

### 2. Decision: ไม่เข้าจอดตามเวลา → ยกเลิกอัตโนมัติ
```
หากไม่เข้าจอดภายใน 30 นาที:
✓ ระบบยกเลิกการจองอัตโนมัติ
✓ ปลดล็อคที่จอด ให้ผู้อื่นจองได้
✓ ไม่คืนเงินค่าจอง (No-show fee)
✓ ส่ง Notification แจ้งเตือนผู้ใช้
```

### 3. Decision: จอดเกินเวลา → คิดค่าเพิ่ม
```
หากจอดเกินเวลาที่จอง:
✓ คำนวณค่าบริการเพิ่ม (10 ฿/ชม.)
✓ หักเงินอัตโนมัติผ่านแอป
✓ แสดง Notification ค่าใช้จ่ายเพิ่ม
✓ บันทึกในประวัติการใช้งาน
```

---

## 🚀 Future Features (Smart Campus)

### Phase 4+: IoT & AI Integration
```
📡 IoT Sensors
   ├─ ตรวจจับรถอัตโนมัติ
   └─ อัปเดตสถานะ Real-time

📹 CCTV + AI
   ├─ License Plate Recognition
   └─ จับการจอดผิดที่

🤖 Predictive Analytics
   ├─ ทำนาย Peak Hours
   ├─ แนะนำเวลาที่เหมาะสม
   └─ Dynamic Pricing

📊 Parking Analytics
   ├─ วิเคราะห์พฤติกรรมผู้ใช้
   ├─ วางแผนขยายพื้นที่
   └─ ปรับปรุงประสิทธิภาพ
```

---

**Document Version:** 2.0  
**Last Updated:** November 8, 2025  
**Status:** ✅ Complete - Aligned with Business Requirements v2.0
